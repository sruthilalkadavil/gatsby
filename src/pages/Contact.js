import React, { Component } from 'react';
import Layout from './components/Layout';

class Contact extends Component {
    render() {
        return (
            <Layout>
             <h2>texting contact</h2>
            </Layout> 
        );
    }
}

export default Contact;
